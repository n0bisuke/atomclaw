/* atom_secrets.h  ← copy this file to atom_secrets.h and fill in your values
 *
 * NEVER commit atom_secrets.h to git!
 * This file is already in .gitignore.
 */
#pragma once

/* WiFi */
#define ATOM_SECRET_WIFI_SSID           "your-wifi-ssid"
#define ATOM_SECRET_WIFI_PASS           "your-wifi-password"

/* LLM (Anthropic or OpenAI) */
#define ATOM_SECRET_API_KEY             "sk-ant-..."
#define ATOM_SECRET_MODEL               "claude-haiku-4-5"
#define ATOM_SECRET_MODEL_PROVIDER      "anthropic"

/* Discord Application
 * From: https://discord.com/developers/applications
 *   → Your App → General Information
 */
#define ATOM_SECRET_DISCORD_APP_ID      "1234567890123456789"
/* Public Key (64-char hex) - from Discord Developer Portal */
#define ATOM_SECRET_DISCORD_PUBLIC_KEY  "abcdef1234567890abcdef1234567890abcdef1234567890abcdef1234567890"

/* Cloudflare Worker
 * Deploy cloudflare-worker/worker.js and set the URL here.
 * e.g. "https://atomclaw-history.yourname.workers.dev"
 */
#define ATOM_SECRET_CF_WORKER_URL       "https://atomclaw-history.yourname.workers.dev"
/* Optional: Bearer token for Worker auth (set via: wrangler secret put AUTH_TOKEN) */
#define ATOM_SECRET_CF_AUTH_TOKEN       ""

/* Brave Search API (optional - enables web_search tool) */
#define ATOM_SECRET_SEARCH_KEY          ""

/* HTTP Proxy (optional - if your network needs a proxy to reach APIs) */
#define ATOM_SECRET_PROXY_HOST          ""
#define ATOM_SECRET_PROXY_PORT          ""
